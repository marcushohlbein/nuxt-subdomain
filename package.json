{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "docker:build": "docker build -t nuxt-app .",
    "docker:run": "docker run -p 3000:3003 nuxt-app"
  },
  "dependencies": {
    "nuxt": "^3.12.4",
    "vue": "latest"
  }
}
